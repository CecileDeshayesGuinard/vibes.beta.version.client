var TO_STRING_TAG_SUPPORT = require('coa/core-js/internals/to-string-tag-support');
var defineBuiltIn = require('coa/core-js/internals/define-built-in');
var toString = require('coa/core-js/internals/object-to-string');

// `Object.prototype.toString` method
// https://tc39.es/ecma262/#sec-object.prototype.tostring
if (!TO_STRING_TAG_SUPPORT) {
  defineBuiltIn(Object.prototype, 'toString', toString, { unsafe: true });
}
